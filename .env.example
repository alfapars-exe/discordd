# ========================
# mqvi Server Configuration
# ========================
# Bu dosyayı .env olarak kopyalayıp değerlerini doldurun.
# cp .env.example .env

# ─── Server ───
SERVER_HOST=0.0.0.0
SERVER_PORT=9090

# ─── Database ───
DATABASE_PATH=./data/mqvi.db

# ─── JWT ───
# ÖNEMLİ: Production'da güçlü random bir secret kullanın!
# openssl rand -hex 32
JWT_SECRET=change-me-to-a-random-secret-in-production
JWT_ACCESS_EXPIRY_MINUTES=15
JWT_REFRESH_EXPIRY_DAYS=7

# ─── Encryption ───
# LiveKit credential'larını DB'de şifrelemek için AES-256 key (64 hex karakter = 32 byte)
# openssl rand -hex 32
ENCRYPTION_KEY=change-me-run-openssl-rand-hex-32

# ─── File Upload ───
UPLOAD_DIR=./data/uploads
UPLOAD_MAX_SIZE=26214400

# ─── CORS (opsiyonel) ───
# Ek origin'ler, virgülle ayrılmış.
# CORS_ORIGINS=https://yourdomain.com

# ─── LiveKit (opsiyonel — sadece full self-host) ───
# Bunlar sadece ilk kurulumda otomatik seed olarak kullanılır.
# Sunucu başladığında DB'de hiç LiveKit instance yoksa bu değerlerle bir tane oluşturulur.
# Sonraki yönetim admin panelden yapılır. Boş bırakılırsa seed atlanır.
# LIVEKIT_URL=ws://localhost:7880
# LIVEKIT_API_KEY=devkey
# LIVEKIT_API_SECRET=devsecret
