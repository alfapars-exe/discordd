{
  "name": "hichat-e2e",
  "private": true,
  "version": "0.0.0",
  "description": "End-to-end tests for HiChat! (real Go server + Vite dev client)",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0"
  }
}
